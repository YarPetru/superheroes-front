export const getHeroes = state => state.heroes;

export const getCurrentPage = state => state.heroes.currentPage;

export const getCurrentHero = state => state.heroes.currentHero;

export const getLoadingState = state => state.heroes.isLoading;
